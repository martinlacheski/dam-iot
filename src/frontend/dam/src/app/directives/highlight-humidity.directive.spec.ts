import { HighlightHumidityDirective } from './highlight-humidity.directive';

describe('HighlightHumidityDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHumidityDirective();
    expect(directive).toBeTruthy();
  });
});
